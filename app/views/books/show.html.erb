<div class="mx-auto mt-12 grid gap-5 w-1/2">
  <div class="flex flex-col overflow-hidden rounded-lg shadow-lg">
    <div class="flex-shrink-0">
      <img class="h-48 w-full object-cover" src="https://upload.wikimedia.org/wikipedia/en/thumb/a/a9/The_Hobbit_trilogy_dvd_cover.jpg/220px-The_Hobbit_trilogy_dvd_cover.jpg" alt="">
    </div>
    <div class="flex flex-1 flex-col justify-between bg-white p-6">
      <div class="flex-1">
        <p class="text-sm font-medium text-indigo-600">
          <a href="#" class="hover:underline">Fantasy</a>
        </p>
        <a href="#" class="mt-2 block">
          <p class="text-xl font-semibold text-gray-900">
            <%= @book_view_model.book_title %>
          </p>
          <p class="mt-3 text-base text-gray-500">
            The Hobbit is a children's fantasy novel by English author J. R. R. Tolkien. It was
            published on 21 September 1937 to wide critical acclaim, being nominated for the
            Carnegie Medal and awarded a prize from the New York Herald Tribune for best juvenile
            fiction. The book remains popular and is recognized as a classic in children's
            literature.
          </p>
        </a>
      </div>
      <div class="mt-6 flex items-center">
        <div class="flex-shrink-0">
        </div>
        <div class="ml-3">
          <p class="text-sm font-medium text-gray-900">
            <a href="#" class="hover:underline">
              <%= @book_view_model.book_author %>
            </a>
          </p>
          <div class="flex space-x-1 text-sm text-gray-500">
            <span>1006 min read</span>
          </div>
        </div>
      </div>

      <dl class="sm:divide-y sm:divide-gray-200 mt-4">
        <div class="py-4 sm:grid sm:grid-cols-3 sm:gap-4 sm:py-5">
          <dt class="text-sm font-medium text-gray-500">Reservations <%= @book_view_model.reservations.count %>:</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:col-span-2 sm:mt-0">
            <ul>
              <% @book_view_model.reservations.each do |reservation| %>
                <li class="mb-2">
                  <%= reservation.user.full_name %>
                </li>
              <% end %>
            </ul>
          </dd>
        </div>
      </dl>
      <dl class="sm:divide-y sm:divide-gray-200 mt-4">
        <div class="py-4 sm:grid sm:grid-cols-3 sm:gap-4 sm:py-5">
          <dt class="text-sm font-medium text-gray-500">Borrow history <%= @book_view_model.borrow_histories.count %>:</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:col-span-2 sm:mt-0">
            <ul>
              <% @book_view_model.borrow_histories.each do |history| %>
                <li class="mb-2 flex flex-col">
                  <span class="font-bold"><%= history.user_name %></span>
                  <p>
                    <span>Borrowed at:</span> <%= history.borrow_date %>
                    <span>Returned at:</span> <%= history.return_date %>
                  </p>
                </li>
                <hr />
              <% end %>
            </ul>
          </dd>
        </div>
      </dl>
    </div>
  </div>
</div>
